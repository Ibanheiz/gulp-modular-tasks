(function (angular) {
  'use strict';

  var _site = {
  };

  // Functions
  function SiteController($scope, $http) {
  }

  // Controllers
  angular.module('app.modules.Site.controllers', [])
    .controller('SiteController', SiteController);

  // Inject
  SiteController.$inject = ['$scope', '$http'];

}(angular));


